{
  "name": "mapbox-embed-vue",
  "version": "0.1.0",
  "scripts": {
    "dev": "pnpm run --parallel dev",
    "build": "pnpm run --parallel build",
    "preview": "pnpm run --parallel preview",
    "test": "pnpm run --parallel test",
    "lint": "pnpm run --parallel lint",
    "lint:fix": "pnpm run --parallel lint:fix",
    "typecheck": "pnpm run --parallel typecheck",
    "prepublish": "pnpm --filter @mxwllstn/mapbox-embed-vue run prepublish",
    "release": "pnpm run lint && pnpm run test && pnpm run build && pnpm --filter @mxwllstn/mapbox-embed-vue run release",
    "compile": "pnpm --filter @mxwllstn/mapbox-embed-vue run compile",
    "cli": "pnpm --filter @mxwllstn/mapbox-embed-vue run cli"
  },
  "devDependencies": {
    "@antfu/eslint-config": "^5.4.1",
    "@vue/tsconfig": "^0.8.1",
    "eslint": "^9.36.0",
    "typescript": "^5.9.2",
    "vue": "^3.5.21"
  }
}
